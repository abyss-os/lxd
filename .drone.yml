kind: pipeline
name: default

steps:
- name: build image
  image: abyssos/abyss
  commands:
  - /bin/sh ./build.sh

- name: deploy image
  image: minio/mc
  commands:
  - /bin/sh ./upload.sh mc
  when:
    branch:
    - master
